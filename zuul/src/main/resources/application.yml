server:
  port: 8885
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8880/eureka/
spring:
  application:
    name: zuul
zuul:
  routes:
    api-a:
      path: /api/**
      serviceId: SERVICE2